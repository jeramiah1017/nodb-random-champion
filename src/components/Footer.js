import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>ADC (Attack Damage Carry) is an archaic term used to refer to a champion that deals strong, continuous damage with their basic attacks and scales with attack-related stats - i.e. attack damage, critical strike chance and attack speed.</p>
        
        </footer>
    );
};

export default Footer;